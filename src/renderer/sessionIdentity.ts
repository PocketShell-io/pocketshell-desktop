/**
 * The session tree algebra lives in @pocketshell/core now — one implementation
 * for the desktop renderer, the web workspace, and tests. This file keeps the
 * renderer's import paths (and this history's blame anchors) stable.
 */
export * from '@pocketshell/core';
