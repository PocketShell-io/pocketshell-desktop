{
  "name": "@pocketshell/ui",
  "private": true,
  "type": "module",
  "source": "src/index.ts",
  "scripts": {
    "build": "vite build --config vite.config.ts",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit"
  },
  "description": "Shared Vue UI source for PocketShell desktop and Android"
}
